# ndn-lite-atu
In thin NDN Lite Implementation for Contiki NG, a small bug in NDN-Lite is fixed, It became multi-hop. RONR is implemenmted byh default with incorrect PIT, FIB Timeout. VIF can be added as alternative. A simple energy module is added when packets are being transmitted or received. Alternartively, Contiki NG's energst module can be used.
